import { PlusCircle as Icon } from 'lucide-react'

import { IconProps } from '@/components/icons'

export const UploadPlus = ({ color, size, strokeWidth }: IconProps) => {
  return <Icon color={color} size={size} strokeWidth={strokeWidth} />
}
