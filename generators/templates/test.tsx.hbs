import { render } from '@testing-library/react'

import  { {{pascalCase name}} } from '@/components/{{camelCase name}}'

describe('<{{pascalCase name}} />', () => {
  it('should render the heading', () => {
    const { getByRole } = render(<{{pascalCase name}} />)
    const heading = getByRole('heading', { name: /{{camelCase name}}/i })

    expect(heading).toBeInTheDocument()
  })
})
